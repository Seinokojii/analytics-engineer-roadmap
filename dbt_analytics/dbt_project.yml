name: 'analytics_project'
version: '1.0.0'
config-version: 2

profile: 'analytics'

model-paths: ["models"]
macro-paths: ["macros"]
seed-paths: ["data"]
test-paths: ["tests"]

models:
  analytics_project:
    staging:
      +materialized: view
    marts:
      +materialized: table